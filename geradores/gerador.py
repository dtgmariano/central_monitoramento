from hl7parser import channel
class gerador:
	def info(self, number, unit, limits, label, resolution,fs):
		self.chn = channel(number, label, resolution, unit, fs, limits, self)
	def getNext():
		pass
		
